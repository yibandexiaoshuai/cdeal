-module(market_c).
-export([current_price/0]).

current_price()->
    nil.

buy(Amount, Price)->
    nil.

sell(Amount, Price)->
    nil.

order(Type, Amount, Price)->
    nil.

get_account_status()->
    nil.
